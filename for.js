const numeros = [100, 200, 300, 400, 500, 600];


    //exe 1x        // cond for exe            //exe final block
for (let indice = 0; indice < numeros.length; indice = indice++) {
    console.log(numeros[indice]);
}